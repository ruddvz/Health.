<div class="strip mono-caps" role="status" aria-live="off">
	<span class="brand mono">HEALTH</span>
</div>

<style>
	.strip {
		display: flex;
		align-items: center;
		justify-content: center;
		gap: var(--space-2);
		min-height: 24px;
		padding: var(--space-2) 0 var(--space-1);
		font-size: 10px;
		font-weight: 600;
		letter-spacing: 0.14em;
		text-transform: uppercase;
		color: var(--text-3);
		font-family: var(--font-mono);
	}

	.brand {
		color: var(--text-2);
	}
</style>
